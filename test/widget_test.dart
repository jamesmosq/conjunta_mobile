import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (tester) async {
    // Tests de widgets se agregan por módulo con /modulo-flutter
    expect(true, isTrue);
  });
}
